extends ../layout

block content
  .row
    .col.s12.mb-2
      .table-responsive
        table
          thead
            tr
              th ID
              th username
              th group
              th 수정일
              th 생성일
          tbody
            each user in users
              tr
                td=user.id
                td
                  a(href=`/users/${user.id}`)=user.username
                td=user.group.name
                td=moment(user.updatedAt).format("yyyy/MM/DD HH:mm")
                td=moment(user.createdAt).format("yyyy/MM/DD HH:mm")

    .col.s12.right-align
        a.waves-effect.waves-light.btn.modal-trigger(href='/users/new')
          i.material-icons.left add
          | 신규 사용자 생성